# Optional Python dependencies for repo tooling and plotting scripts.
# Core plugin/pipeline tools (for example plugin/tm-resolve-opaque.py) use only
# Python 3.8+ standard-library modules.
# Requires: python>=3.8.
numpy>=1.21
pandas>=1.3
matplotlib>=3.5
