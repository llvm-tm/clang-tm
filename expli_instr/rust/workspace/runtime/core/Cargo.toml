[package]
name = "runtime-core"
version = "0.1.0"
edition = "2021"

[dependencies]
addrspace = { path = "../../addrspace" }
serde = { version = "1", features = ["derive", "alloc"], optional = true }

[features]
simulation = []
serde = ["dep:serde"]
