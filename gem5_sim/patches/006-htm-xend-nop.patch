diff --git a/src/arch/x86/insts/htmruby.cc b/src/arch/x86/insts/htmruby.cc
index cf88d6c6b6..99c08b89ad 100644
--- a/src/arch/x86/insts/htmruby.cc
+++ b/src/arch/x86/insts/htmruby.cc
@@ -90,8 +90,12 @@ XEndInst::initiateAcc(ExecContext *xc, trace::InstRecord *traceData) const
     Fault fault = NoFault;
     const uint64_t htm_depth = xc->getHtmTransactionalDepth();
 
+    // SE-mode fallback: if a transaction aborted (e.g. syscall inside HTM)
+    // the compiler-generated fallback path may still execute XEND. Real
+    // hardware would #GP, but for SE simulation we treat it as a no-op
+    // to avoid panic and let the C++ fallback (SGL) run.
     if (!xc->inHtmTransactionalState())
-        fault = std::make_shared<InvalidOpcode>();
+        return NoFault;
 
     if (fault == NoFault) {
         Request::Flags memAccessFlags =
@@ -114,6 +118,9 @@ XEndInst::completeAcc(PacketPtr pkt, ExecContext *xc,
     ThreadContext *tc = xc->tcBase();
     const uint64_t htm_depth = xc->getHtmTransactionalDepth();
 
+    if (!xc->inHtmTransactionalState())
+        return NoFault;
+
     getMemLE(pkt, Mem, traceData);
 
     if (Mem != 0)
