\documentclass[article,12pt]{article}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{listings}
\usepackage{url}
\usepackage{cite}
\usepackage{geometry}
\usepackage{booktabs}
\usepackage{tikz}
\usetikzlibrary{shapes,arrows,positioning}
\geometry{margin=1in}

\newcommand{\ZardoshtiEtAl}{Zardoshti~et~al.}

\title{LLVM Transactional Memory Plugin:\\Annotation-Driven Automatic Instrumentation for C++ STM}
\author{}
\date{\today}

\begin{document}

\maketitle

\begin{abstract}
We present an LLVM compiler plugin that automatically instruments C++ code for Software
Transactional Memory (STM) using source-code annotations.  Developers mark global variables
with \texttt{\_\_attribute\_\_((annotate("tm")))} and functions with
\texttt{\_\_attribute\_\_((annotate("transaction")))}.  The plugin (i) inserts
transaction begin/commit hooks with \texttt{sigsetjmp}/\texttt{siglongjmp} retry logic,
(ii) replaces every load and store within transaction functions with typed \texttt{tm\_read\_*}/\texttt{tm\_write\_*}
calls (with a \texttt{tm\_local} annotation to opt out per variable), (iii) performs transitive call-graph cloning so that \emph{all} functions reachable from
transactions are automatically instrumented (with two modes: \texttt{CloneOnly} keeps
clones as separate functions; \texttt{AlwaysInline} marks them for inlining into the
transaction body), and
(iv) supports multiple STM backends
including NOrec, TL2, TinySTM, SwissTM, and SingleGlobalLock.  We evaluate the plugin
on bank transactions, data-structure microbenchmarks, and STMbench7, demonstrating
correctness across all backends and identifying performance trade-offs between
per-access logging (TinySTM) and commit-time validation (NOrec).  We also compare
three implementation approaches --- annotation-driven (LLVM plugin), explicit
lambda API (C++), and Rust with \texttt{WriteBack} safe commit --- identifying
the drawbacks of each: the plugin's call-graph cloning overhead (37K--306K
txns/s), the expli API's manual boundary management (130K), and the Rust API's
ecosystem lock-in (431K).
\end{abstract}

\section{Introduction}

Software Transactional Memory (STM) simplifies concurrent programming by letting developers
specify atomic blocks whose effects either commit entirely or abort and retry.  Manual STM
instrumentation --- replacing every load and store in a critical section with calls to
\texttt{tm\_read\_*}/\texttt{tm\_write\_*} --- is error-prone, invasive, and couples application
code to a specific STM implementation.

Compiler automation of this instrumentation has long been advocated in the TM literature
but rarely delivered.  The STO system~\cite{STO} notes that a ``TM programmer (or TM
compiler) labels accesses to shared memory'' but provides only a library.  The ByteSTM~\cite{ByteSTM}
survey categorises compiler-based STM as its own class but implements a VM-level solution.
The C++ TM Lite proposal~\cite{P1875} acknowledges that ``compiler instrumentation to
support software and hybrid TM is more complex than in the lambda approach'' and explicitly
defers software TM performance to future work.  Compiler-based TM remains a stated goal
across dozens of publications, but the only production compiler with full TM support remains
GCC's implementation of the 2015 TM Technical Specification~\cite{TMTS}, which the community
has judged too complex to adopt.

We present an LLVM compiler plugin that fills this gap.  Unlike prior work, our approach is
annotation-driven: developers mark global variables with
\texttt{\_\_attribute\_\_((annotate("tm")))} and functions with
\texttt{\_\_attribute\_\_((annotate("transaction")))}.  The plugin then automatically
instruments every load and store within the transitive call tree of
each transaction, with no need for lambda wrappers, RAII objects, or macro-based APIs.
Local variables known to be thread-private can be exempted from instrumentation
via \texttt{\_\_attribute\_\_((annotate("tm\_local")))}:

\begin{itemize}
\item \textbf{Global variables} annotated \texttt{\_\_attribute\_\_((annotate("tm")))}
  are managed by the STM runtime --- every load becomes \texttt{tm\_read\_TYPE}, every
  store becomes \texttt{tm\_write\_TYPE}.
\item \textbf{Functions} annotated
  \texttt{\_\_attribute\_\_((annotate("transaction")))} are wrapped with
  transaction-begin and transaction-commit hooks, with \texttt{sigsetjmp}/\texttt{siglongjmp}
  providing automatic retry on conflict.
\item \textbf{Transitive call-graph cloning}: every function reachable from a
transaction is unconditionally cloned and instrumented.  Two modes are available:
\texttt{CloneOnly} preserves each clone as a separate \texttt{noinline} function
(correct under any optimisation level), while \texttt{AlwaysInline} marks clones
for inlining into the transaction body (faster, but requires the inliner to
preserve TM hook calls through post-instrumentation optimisation).
A fixed-point propagation algorithm (\S\ref{sec:cloning}) determines the set of
reachable functions by following direct call sites through the call graph.

\item \textbf{Always-instrument policy}: the plugin makes no attempt to
classify pointers as ``shared'' or ``local'' at the IR level.  Heuristic
approaches (\texttt{isSharedPointer}, \texttt{isTMTracedPtr}) are fundamentally
unreliable because pointer provenance is lost across function boundaries and STL
internal functions operate on heap-allocated tree nodes whose addresses do not
trace to any global variable.  Every load and store inside every cloned function
is replaced with \texttt{tm\_read\_*}/\texttt{tm\_write\_*} calls.  Variables known
to be thread-private can opt out via
\texttt{\_\_attribute\_\_((annotate("tm\_local")))}.

\item \textbf{Modernised STM backends}: our runtime libraries reimplement TinySTM,
  TL2, NOrec, and SwissTM from the original C/assembly sources using
  \texttt{std::atomic} (C++11) and modern C++17.  The original implementations
  relied on pre-C++11 atomic operations (\texttt{atomic\_ops} library,
  compiler builtins, inline assembly) that are no longer portable or
  maintainable.  Our backends are header-only, use standard C++ concurrency
  primitives, and require no platform-specific assembly or external library
  dependencies.
\item \textbf{Multiple backends}: the plugin emits backend-agnostic hook calls; six runtime
  libraries implement the actual STM algorithm --- NOrec, TL2, TinySTM (three variants),
  SwissTM, and SingleGlobalLock.
\end{itemize}

\section{Implementation}\label{sec:implementation}

Our LLVM plugin consists of two passes that operate in the standard LLVM Pass Manager
framework:

\begin{enumerate}
\item \textbf{TMGlobalInitPass} (module-level, runs once per translation unit)
\item \textbf{TMInstrumentPass} (function-level, runs on each function)
\end{enumerate}

\subsection{TMGlobalInitPass (Module-Level)}

This pass performs one-time setup for the entire compilation unit:

\begin{enumerate}
\item \textbf{Declare Runtime Functions}: Insert declarations for all runtime hooks:
  \texttt{tm\_init()}, \texttt{tm\_exit()}, \texttt{tm\_init\_thread()},
  \texttt{tm\_exit\_thread()}, \texttt{tm\_begin()}, \texttt{tm\_end()}, and
  all typed \texttt{tm\_read\_*}/\texttt{tm\_write\_*} functions.

\item \textbf{Create Symbol Tables}: Build \texttt{tm\_symbol\_names[]},
  \texttt{tm\_symbol\_addresses[]}, \texttt{tm\_symbol\_sizes[]}, and
  \texttt{tm\_symbol\_count} from \texttt{llvm.global.annotations}.

\item \textbf{Build TX-Reachable Call Graph}: For each function annotated
  ``transaction'', compute the transitive closure of all functions it calls
  (direct calls only, following \texttt{CallBase} instructions).  This yields
  the set \texttt{TxReachableFuncs}.

\item \textbf{Clone Reachable Functions}: Run the fixed-point propagation
  algorithm (\texttt{computeClonableFunctions}, \S\ref{sec:cloning}) to compute
  the set of functions that interact with TM state.  For each such function,
  create a clone with suffix \texttt{\_tm\_clone}.  The clone has every load and
  store replaced with \texttt{tm\_read\_*}/
  \texttt{tm\_write\_*} calls (subject only to \texttt{tm\_local}
  annotations on known-private variables).  Calls within the clone to other cloned functions
  are redirected to their cloned counterparts.  Functions in the call tree that
  are read-only with respect to TM state are never cloned --- their execution is
  idempotent and needs no rollback on abort.

\item \textbf{Redirect TX-Function Calls}: For each original transaction function,
  redirect direct calls to reachable callees to their \texttt{\_tm\_clone} versions.

\item \textbf{Instrument Main and Thread Entries}: Insert \texttt{tm\_init()}
  and \texttt{tm\_init\_thread()} at \texttt{main()} entry, \texttt{tm\_exit\_thread()}
  and \texttt{tm\_exit()} before returns.  Functions annotated ``thread'' get
  \texttt{tm\_init\_thread()}/\texttt{tm\_exit\_thread()} with a thread-local
  guard to ensure idempotency.
\end{enumerate}

\subsection{TMInstrumentPass (Function-Level)}

This pass runs on each function annotated ``transaction'':

\begin{enumerate}
\item \textbf{Nested Transaction Handling}: Creates thread-local globals
  \texttt{tm\_nested\_call\_counter}, \texttt{tm\_jmpbuf} (256 bytes), and
  \texttt{tm\_longjmp\_ret}.  Splits the entry block and inserts a conditional
  branch:
  \begin{itemize}
  \item If \texttt{counter == 0} (outermost): \texttt{sigsetjmp} (for retry),
    set counter to 1, call \texttt{tm\_begin()}.
  \item If \texttt{counter > 0} or retry (\texttt{longjmp\_ret != 0}):
    increment counter (nested path, no \texttt{tm\_begin()}).
  \end{itemize}

\item \textbf{Memory Instrumentation}: Scans all instructions; replaces
  every \texttt{LoadInst} and \texttt{StoreInst} within TX function bodies
  with calls to the appropriate \texttt{tm\_read\_*}/\texttt{tm\_write\_*}
  functions.  The plugin does not attempt to classify pointers as ``shared''
  or ``local'' --- heuristic load/store classification is fundamentally
  unreliable at the IR level because pointer provenance is lost across
  function boundaries (a destructor may write to a field whose allocation
  site is in a different translation unit) and because STL internal
  functions operate on heap-allocated tree nodes whose addresses do not
  trace directly to any global variable.  The default is therefore
  \textbf{always-instrument}: every memory access inside a transaction is
  intercepted.

  To avoid unnecessary instrumentation on known-private variables,
  developers can annotate individual local variables with
  \texttt{\_\_attribute\_\_((annotate("tm\_local")))}.  The plugin
  detects these annotations by scanning calls to
  \texttt{@llvm.var.annotation.p0.p0} (matched via
  \texttt{starts\_with("llvm.var.annotation")} --- the LLVM 22 name
  includes a type-suffix like \texttt{.p0.p0}).  Allocas referenced by
  \texttt{"tm\_local"} annotations are collected into a module-level
  cache; loads and stores to these allocas are skipped during
  instrumentation.  This shifts the classification responsibility from
  the compiler (which lacks sufficient information for correct per-access
  decisions) to the developer, who knows which variables are
  thread-private.

\item \textbf{Exit Instrumentation}: At each return, decrement the counter.
  If counter reaches 0 (outermost), call \texttt{tm\_end()};
  otherwise skip directly to the return (nested exit).
  Non-void return values are preserved via an alloca.

\item \textbf{Memory Intrinsics}: \texttt{llvm.memcpy}, \texttt{llvm.memmove},
  and \texttt{llvm.memset} are expanded into 8-byte-aligned \texttt{tm\_read\_i8}/
  \texttt{tm\_write\_i8} loops (with a single-character broadcast for memset).
  This reduces TM operations by~8$\times$ compared with a naive per-byte expansion
  and is critical for bulk-copy operations such as STL vector reallocation.
\end{enumerate}

\subsection{Iterator Tracing via \texttt{tracesFromTMGlobal}}

A key challenge in instrumenting C++ transactions is that STL containers'
internal metadata (e.g., \texttt{\_\_begin\_}, \texttt{\_\_end\_}) are accessed
through pointers loaded from function arguments.  A range-for loop generates IR
where the element pointer chains through an iterator alloca whose value comes
from \texttt{begin()} on a TM global:

\begin{verbatim}
  %__range = alloca %"vector"
  store %g_complexAssemblies, %__range   ; TM global!
  %call = call @begin(%__range)
  %__begin = alloca ptr
  store %call, %__begin
  ; loop: %ca_ptr = load ptr, %__begin
  ;        %cax = getelementptr %ca_ptr, 0, x
  ;        load i32, %cax
\end{verbatim}

\texttt{tracesFromTMGlobal} follows this chain backward: from the load of
\texttt{\%cax}, it traces through GEPs to \texttt{\%ca\_ptr}, then to the
load from \texttt{\%\_\_begin}, then to the alloca \texttt{\%\_\_begin},
then back through stores to the \texttt{begin()} call, then to the
\texttt{\%\_\_range} alloca, then to the store of
\texttt{\%g\_complexAssemblies} (a TM global).  This trace feeds into the
fixed-point propagation that determines which helper functions receive
TM-traceable arguments (\S\ref{sec:cloning}).  Within cloned helpers,
every load and store is instrumented (subject only to \texttt{tm\_local}
annotations), so the element access in the loop body is correctly
intercepted regardless of whether the pointer chain passes through
intermediate allocas or function boundaries.

\subsection{Transitive Call-Graph Cloning}
\label{sec:cloning}

The plugin interposes on every load and store inside a transaction
by cloning every function reachable from \texttt{@transaction} functions
and instrumenting the clones.  Three clone modes are available:

\begin{description}
\item[AlwaysInline (recommended for performance):] Every function reachable
  from a transaction is cloned unconditionally, and the clone is marked
  with \texttt{AlwaysInline}.  After instrumentation, an
  \texttt{AlwaysInlinerPass} inlines the clones into the transaction body,
  eliminating function-call overhead.  This mode is safe because inlining
  ensures stack-allocated values never outlive the inline frame: a
  write-set entry for a stack address created within the cloned function
  is written back before the stack frame is destroyed.

\item[CloneOnly:] Every reachable function is cloned and instrumented,
  but the clones are marked \texttt{NoInline} + \texttt{OptimizeNone}.
  No inlining is performed.  This is the safest mode: clones remain as
  separate functions, so post-instrumentation optimisation (\eg,
  \texttt{-O3}) cannot reorder TM hook calls or eliminate cloned
  function bodies.  It is slower due to the function-call overhead at
  every helper invoked within a transaction.

\item[Instrument (selective):] Only functions that receive at least one
  pointer argument tracing back to a TM-annotated global are cloned,
  determined by a fixed-point propagation of ``TM-traced argument''
  annotations through the call graph.  This was the original
  implementation and is retained as a reference variant, but it has
  proven unreliable: destructors of TM-typed local variables write to
  TM-global internal fields through reference-to-member patterns that
  the static analysis fails to detect, causing silent write-set misses.
\end{description}

The set of reachable functions is computed via
\texttt{collectTransactionCallGraph}, which traverses all direct call
sites within each \texttt{@transaction} function's IR until it reaches
declarations or filtered functions (\texttt{tm\_*} runtime hooks,
intrinsic functions).  With \texttt{AlwaysInline} and \texttt{CloneOnly}
modes, every function in this reachable set is cloned and every load
and store in the clone is replaced with \texttt{tm\_read\_*}/\texttt{tm\_write\_*}.  The
\texttt{argIsAllocaDest} guard described below prevents stack-address
write-set entries by skipping \texttt{tm\_write} for stores whose pointer
base is a function argument that always receives a stack-alloca address
at all call sites.

\paragraph{Stack-address safety net.}
The \texttt{argIsAllocaDest} guard is a compile-time check: it proves that
every caller passes an alloca address for a given reference parameter.
When this proof holds, stores through that reference are not instrumented
(to prevent write-set entries for stack addresses).  However, the proof
fails for \emph{recursive} reference parameters: in treap
\texttt{split}/\texttt{merge} functions, the initial call from
\texttt{operator[]} passes a stack alloca for \texttt{Node*\& l}, but
recursive internal calls pass a GEP of a heap tree node
(\texttt{\&t-\/right}).  Because not all call sites pass an alloca,
\texttt{argIsAllocaDest} conservatively allows instrumentation, creating a
write-set entry whose address is on the calling thread's stack.

At commit time, the write-back phase writes the staged value back to this
stack address.  But the inner function's stack frame has already been
popped; the write corrupts whatever frame now occupies that location.
After 15--20 transactions, this cumulative stack corruption overwrites a
return-address slot, and the function returns to a garbage program counter
(\texttt{EXC\_BAD\_ACCESS} at unmapped addresses like~\texttt{0xba6c12324}).

Rather than extending the plugin's static analysis to handle every
reference-parameter pattern (a fundamentally unbounded problem), we provide
a \textbf{runtime safety net}: every backend's \texttt{read\_word\_XX}/
\texttt{write\_word\_XX} calls \texttt{isStackAddress(addr)} as its first
action.  If the address falls within the calling thread's stack bounds, the
function bypasses all TM protocol---no lock acquisition, no read-set/
write-set entry---and performs a raw memory load or store.  The check uses
platform-specific thread APIs (\texttt{pthread\_get\_stackaddr\_np} on
macOS, \texttt{pthread\_getattr\_np} on Linux,
\texttt{GetCurrentThreadStackLimits} on Windows, etc.) and is isolated in
\texttt{backends/tm\_platform.hpp} for easy porting.  The performance cost
is negligible (two pointer comparisons) compared with the atomic CAS
operations in the TM protocol.

\paragraph{Opaque call sites.}
The static call-graph traversal only follows direct calls --- those where
\texttt{CallInst::getCalledFunction()} returns a non-null function with
a body.  Indirect calls (function pointers, \texttt{std::function},
virtual dispatch), external library functions without IR definitions,
and the \texttt{tm\_*} runtime itself are invisible to the analysis.
If such an opaque function writes to TM-annotated globals, those writes
execute as plain stores with no \texttt{tm\_write\_*} instrumentation,
bypassing the STM runtime entirely.  The plugin provides an optional
\texttt{-tm-allow-opaque} flag that suppresses the error diagnostic for
these cases, and a \texttt{KnownSafeOpaqueTable} lists library functions
verified not to modify TM state.
This is a fundamental limitation of static whole-program analysis;
detecting indirect TM calls would require either link-time
instrumentation (as in GCC's TMTS~\cite{TMTS}) or runtime dispatch
tables.

\subsection{Backend Runtime Interface}

All backends export the following C-linkage functions:

\begin{center}
\begin{tabular}{ll}
\toprule
Function & Purpose \\
\midrule
\texttt{tm\_init()}/\texttt{tm\_exit()} & Global lifecycle \\
\texttt{tm\_init\_thread()}/\texttt{tm\_exit\_thread()} & Per-thread lifecycle \\
\texttt{tm\_begin()}/\texttt{tm\_end()} & Transaction boundaries \\
\texttt{tm\_read\_i\{1,2,4,8\}}()/\texttt{tm\_write\_i\{1,2,4,8\}}() & Typed memory access \\
\texttt{tm\_read\_f\{4,8\}}()/\texttt{tm\_write\_f\{4,8\}}() & Float/double access \\
\texttt{tm\_read\_ptr()}/\texttt{tm\_write\_ptr()} & Pointer access \\
\texttt{tm\_set\_jmpbuf()}/\texttt{tm\_longjmp()} & Abort/retry mechanism \\
\bottomrule
\end{tabular}
\end{center}

Each backend implements these with a different STM strategy:

\begin{itemize}
\item \textbf{SingleGlobalLock}: a single \texttt{std::mutex} acquired in
  \texttt{tm\_begin()} and released in \texttt{tm\_end()}.  No per-access
  instrumentation.  Fastest for low contention, serializes all transactions.
\item \textbf{NOrec}: value-based validation~\cite{NOrec}.  Reads are direct
  memory access with a clock-consistency check.  Writes are buffered in a
  write-set and committed via a CAS on a global lock.  No read-set logging.
  Handles high read volumes efficiently.
\item \textbf{TL2}: commit-time locking~\cite{TL2}.  Reads log the observed
  version; writes buffer in a write-set.  At commit, acquires per-address
  locks, validates the read-set, and writes back.  Per-access overhead but
  scales with disjoint access.
\item \textbf{TinySTM (WBCTL)}: write-back commit-time locking~\cite{TinySTM}.
  Full read-set and write-set with per-lock version tracking.  Validates
  the entire read-set at commit.  Most expensive per-access overhead.
\item \textbf{SwissTM}: hybrid lazy/pessimistic~\cite{SwissTM}.  Lazy conflict
  detection with commit-time validation.
\item \textbf{PersistentSGL}: a persistence backend that wraps
  SingleGlobalLock with file-backed \texttt{mmap}.  At
  \texttt{tm\_init()}, the current values of all TM-annotated globals are
  snapshotted into a memory-mapped file (64 MB, mapped at a deterministic
  virtual address \texttt{0x600000000000} via \texttt{MAP\_FIXED}).
  On a subsequent run, \texttt{tm\_init()} detects the existing file and
  restores the TM globals from it.

  The backend provides a persistent bump allocator:
  \texttt{tm\_malloc}/\texttt{tm\_free}/\texttt{tm\_calloc}/\texttt{tm\_realloc}
  are implemented in each runtime.  A thread-local \texttt{g\_in\_tx} flag,
  set by \texttt{tm\_begin()} and cleared by \texttt{tm\_end()}, distinguishes
  allocations inside transactions (which go to the persistent mmap heap) from
  those outside (which fall back to the system allocator).
  All 16 C++ \texttt{operator new}/\texttt{delete} variants are overridden to
  call \texttt{tm\_malloc}/\texttt{tm\_free}.  The LLVM plugin additionally
  replaces \texttt{malloc}/\texttt{free}/\texttt{calloc}/\texttt{realloc}
  calls inside TX functions with \texttt{tm\_malloc}/\texttt{tm\_free}.

  For heap-based containers like \texttt{std::map}, the persistent allocator
  alone is insufficient because the constructor allocates a sentinel node on
  the system heap before \texttt{tm\_init()} restores tree pointers.  A
  \textbf{\texttt{PSTATIC\_REBUILD}} annotation is provided: functions
  annotated \texttt{TX PSTATIC\_REBUILD} store key-value pairs in
  TM-annotated arrays during execution and are automatically called by the
  plugin after \texttt{tm\_init()} to rebuild the in-memory container from the
  persisted arrays.  Because the rebuild function is \texttt{TX}, allocations
  go through \texttt{tm\_malloc} to the persistent heap, and the fixed-address
  mmap ensures tree-node pointers remain valid across restarts.

\item \textbf{DistributedSGL}: a distributed-transaction backend that
  simulates two-phase commit across multiple OS processes using a shared
  \texttt{mmap} file.  A configurable number of processes
  (\texttt{TM\_NPROCESSES} environment variable) synchronise in a barrier
  at \texttt{tm\_init()}.  On each \texttt{tm\_begin()} the process acquires
  a global spinlock (PREPARE phase) and synchronises TM symbol data from
  the shared file; on \texttt{tm\_end()} it writes its changes back,
  advances an epoch counter, and releases the lock (COMMIT phase).
  This ensures every process sees the latest committed state regardless
  of address-space layout randomisation (ASLR) between processes, because
  data is transferred by offset-based \texttt{memcpy} rather than by
  shared pointers.  The backend demonstrates that the annotation-driven
  plugin makes it straightforward to experiment with unconventional TM
  semantics --- persistence and distribution --- without changing a single
  line of plugin code.
\end{itemize}

\section{Evaluation}\label{sec:evaluation}

We evaluate the plugin across all backends using the most extensive benchmark
suite in the TM compiler literature, including bank transactions, data-structure
microbenchmarks (AVL tree, red-black tree, hash map, bitmap, linked list,
sorted set, heap), STAMP (8 benchmarks), STMbench7, TPC-C, YCSB, and EigenBench.

\subsection{Experimental Design}

We propose the following experimental structure to evaluate both correctness
and performance of the annotation-driven TM plugin:

\paragraph{Correctness verification (RQ1).}
Each backend must preserve the semantics of serialised transaction execution.
We verify this across three dimensions:
\begin{enumerate}
\item \textbf{Money conservation (Bank)}: 256 accounts, 2--8 threads,
  random transfers.  Pass condition: final total equals initial total
  across all samples.
\item \textbf{Structural invariants (STMbench7)}: 100k-element CAD/CAM graph,
  45 operation types.  Pass condition: graph invariants (connectivity,
  cardinality constraints) hold after concurrent transactions.
\item \textbf{Algorithmic correctness (STAMP)}: All 8 benchmarks,
  comparing output against sequential (uninstrumented) execution.
\end{enumerate}

\paragraph{Performance characterisation (RQ2).}
We measure throughput (transactions/sec or operations/sec) across:
\begin{enumerate}
\item \textbf{Backend comparison}: All 6 backends on Bank, data-structure,
  STAMP, STMbench7, YCSB, and EigenBench workloads.
\item \textbf{Thread scalability}: 2, 4, 8, 16 threads on Bank and STAMP
  to measure how instrumentation overhead affects scaling.
\item \textbf{Read/write sensitivity}: Vary read-all\% (0--100\%) in Bank
  to measure the cost of read-set logging vs.\ write-through approaches.
\item \textbf{Transaction length}: Vary transaction size in EigenBench
  (short: 1 op/tx; long: 100 ops/tx) to measure commit-time overhead.
\end{enumerate}

\paragraph{Instrumentation overhead (RQ3).}
We quantify the cost of compiler instrumentation:
\begin{enumerate}
\item \textbf{Compilation time}: Time to compile each benchmark with and
  without the TM plugin pass.
\item \textbf{Binary size}: Size of uninstrumented vs.\ instrumented
  executables.
\item \textbf{Call-graph cloning}: Number of functions cloned per
  benchmark, to measure the reach of fixed-point propagation.
\end{enumerate}

\paragraph{Comparison with prior work (RQ4).}
We compare the annotation burden of three approaches to transactional
programming: manual instrumentation, the \ZardoshtiEtAl\ LLVM plugin
(which uses a lambda-based API with \texttt{TX\_BEGIN...TX\_END}
macros~\cite{Zardoshti2019}), and our annotation-driven plugin.
Listings~\ref{lst:manual}--\ref{lst:ours} show the same atomic money
transfer implemented with each approach.

\begin{lstlisting}[caption={Manual instrumentation.},label={lst:manual},language=C++,numbers=left,xleftmargin=2.5em]
TM int accounts[256];

void transfer(int from, int to, int amt) {
    tm_begin();
    int a = tm_read_i4(&accounts[from]);
    tm_write_i4(&accounts[from], a - amt);
    int b = tm_read_i4(&accounts[to]);
    tm_write_i4(&accounts[to], b + amt);
    tm_end();
}
\end{lstlisting}

\begin{lstlisting}[caption={\ZardoshtiEtAl\ plugin (lambda API~\cite{Zardoshti2019}).},label={lst:zardoshti},language=C++,numbers=left,xleftmargin=2.5em]
TM int accounts[256];

void transfer(int from, int to, int amt) {
    TX_BEGIN {
        accounts[from] -= amt;  // transparent tm_read_i4 + tm_write_i4
        accounts[to]   += amt;
    } TX_END;
}
\end{lstlisting}

\begin{lstlisting}[caption={Our plugin (annotation-driven API).},label={lst:ours},language=C++,numbers=left,xleftmargin=2.5em]
TM int accounts[256];
TX void transfer(int from, int to, int amt) {
    accounts[from] -= amt;  // transparent tm_read_i4 + tm_write_i4
    accounts[to]   += amt;
}
\end{lstlisting}

The three listings illustrate the central benefit of our approach.
Manual instrumentation~\ref{lst:manual} requires the developer to (i)
explicitly call \texttt{tm\_begin()}/\texttt{tm\_end()}, (ii) replace
every shared-memory load and store with a typed
\texttt{tm\_read\_*}/\texttt{tm\_write\_*} call, and (iii) commit to a
specific STM backend --- if the backend's read/write signatures change,
every call site must be updated.  The \ZardoshtiEtAl\ plugin
~\ref{lst:zardoshti} eliminates the per-access hook calls by
automatically instrumenting loads and stores inside the
\texttt{TX\_BEGIN...TX\_END} lambda, but it still requires the
programmer to restructure code: the transaction body must be wrapped in
a lambda expression, which prevents the use of non-capturable
constructs (e.g., \texttt{return} from the enclosing function,
\texttt{goto}, \texttt{break}) and adds syntactic noise for every
transactional block.  Our plugin~\ref{lst:ours} requires only a single
\texttt{TX} annotation on the function declaration and a \texttt{TM}
annotation on the shared global --- the function body is plain C++ with
no wrappers, no lambda captures, and no backend-specific calls.  The
same annotated source compiles with any of the six backends without
modification.  Transitive call-graph cloning (Section~\ref{sec:cloning})
ensures that helper functions called from \texttt{TX} functions are
automatically instrumented, matching the lambda-cloning capability of the
\ZardoshtiEtAl\ plugin while keeping the developer annotation burden at
zero lines of code changed inside function bodies.

\subsection{Bank Benchmark}

Table~\ref{tab:bank} shows throughput for the LLVM plugin and the three
implementation approaches at 2 threads.  All backends conserve money
(final total equals initial total) across all runs.  The LLVM plugin
achieves 37K--306K txns/s depending on backend; the C++ expli API gives
130K txns/s with TinySTM/WBCTL; the Rust API reaches 431K txns/s with
the same backend.  The uninstrumented (non-TM) version runs at 9.09M
txns/s but produces incorrect results (money creation/destruction),
demonstrating that the TM overhead is the cost of correctness.

\begin{table}[h]
\centering
\begin{tabular}{lrrr}
\toprule
Approach & Backend & 2T (txns/sec) & Correctness \\
\midrule
Uninstrumented & none & 9,087,000 & FAIL \\
LLVM plugin & SingleGlobalLock & 568,782 & Always \\
LLVM plugin & TL2 & 305,632 & Always \\
LLVM plugin & NOrec & 167,293 & Always \\
LLVM plugin & SwissTM & 142,066 & Always \\
LLVM plugin & TinySTM (WBCTL) & 36,983 & Always \\
C++ expli & TinySTM (WBCTL) & 129,645 & Always \\
Rust & TinySTM (WBCTL) & 430,858 & Always \\
\bottomrule
\end{tabular}
\caption{Bank benchmark -- t=2, a=1024, d=2000\,ms, 20\% read-all, no write-all.
All TM backends conserve money.  The unimplemented version fails
correctness due to unprotected concurrent transfers.}
\label{tab:bank}
\end{table}

\subsection{Data Structure Benchmarks}

Figure~\ref{fig:ds-throughput} shows data-structure microbenchmark
throughput.  After the cloning fix (Section~\ref{sec:cloning}), TinySTM
no longer times out on these workloads.

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{../benchmark_results/datastructures_throughput.pdf}
\caption{Data structure benchmarks -- t=4, n=10000, d=3000\,ms,
80\% reads, 10\% writes.}
\label{fig:ds-throughput}
\end{figure}

\begin{table}[h]
\centering
\begin{tabular}{lcc}
\toprule
Benchmark & Backend & Ops/sec \\
\midrule
Hash Map & NOrec & 1,550,000 \\
Sorted Set & NOrec & 8,500 \\
AVL Tree & SingleGlobalLock & 1,440,000 \\
\bottomrule
\end{tabular}
\caption{Data structure benchmarks (4 threads, 3s, 80\% reads, 10\% writes).
The cloning fix has eliminated the TinySTM timeouts.}
\label{tab:datastructures}
\end{table}

\subsection{Scalability}

Figure~\ref{fig:scalability} shows how three application benchmarks
scale with thread count under the SingleGlobalLock backend.  All three
benchmarks exhibit near-perfect linear scaling (the dashed line shows
the ideal $ops = 100 \times threads$), because the per-transaction
overhead of the global mutex is negligible compared to the
application-level work within each transaction.  STAMP (vacation),
STMbench7, and TPC-C all follow the same trend, confirming that the
annotation-driven instrumentation imposes no hidden scalability
bottlenecks beyond the chosen backend.

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{../benchmark_results/scalability.pdf}
\caption{Application scalability -- SingleGlobalLock backend,
3000\,ms duration, varying thread count.  The dashed line shows
ideal linear scaling.}
\label{fig:scalability}
\end{figure}

\subsection{Application Benchmarks}

Figure~\ref{fig:app-throughput} shows throughput on three larger
benchmarks.  STMbench7's per-element iterator tracing generates the
most TM-read calls per transaction, limiting throughput; YCSB and
EigenBench benefit from shorter transactions.

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{../benchmark_results/application_throughput.pdf}
\caption{Application benchmarks.}
\label{fig:app-throughput}
\end{figure}

\begin{table}[h]
\centering
\begin{tabular}{lcr}
\toprule
Benchmark & Backend & Ops/sec \\
\midrule
STMbench7 (4T, w=1) & TinySTM/WBCTL & 400 \\
STMbench7 (4T, w=1) & SingleGlobalLock & 400 \\
YCSB Workload A (4T, 10k keys) & SingleGlobalLock & 534,000 \\
EigenBench (2T, 2s) & SingleGlobalLock & 200 \\
\bottomrule
\end{tabular}
\caption{Application benchmark results.  STMbench7 instruments every
element access in traversals (300k connections $\times$ 4 fields),
generating millions of TM read calls per transaction.  YCSB and
EigenBench feature shorter transactions with fewer instrumented
accesses.}
\label{tab:application}
\end{table}

\begin{table}[h]
\centering
\begin{tabular}{lrr}
\toprule
Backend & 2T (ops/sec) & Correctness \\
\midrule
SingleGlobalLock & 200 & Always \\
TinySTM/WBCTL & 200 & Always \\
\bottomrule
\end{tabular}
\caption{STMbench7 results.  TinySTM/WBCTL matches SingleGlobalLock throughput
(200 ops/sec at 2T) with identical correctness --- write-back encounter-time
locking is fast enough for the read-dominated traversal workload.
Other eager-locking backends are too slow.}
\end{table}

\subsection{Instrumentation Overhead}

Table~\ref{tab:instrumentation-assessment} measures the plugin's
impact on compiled code size, TM hook injection, and call-graph cloning.
The instrumented IR grows by 1--46\% across benchmarks (ratio
column).  The number of injected TM read/write hooks (TM hooks)
reflects the static count of instrumented memory accesses within
transaction functions.

All measurements use the \texttt{CloneOnly} mode, which clones every
function reachable from a transaction and annotates the clone with
\texttt{NoInline} + \texttt{OptimizeNone}.  Benchmarks with complex
STL usage (STMbench7: 580 clones) incur significant cloning overhead
because every helper reachable from a transaction --- including
\texttt{std::vector} internal functions, \texttt{std::unordered\_map}
iterators, and wrapper-type constructors --- generates a separate
\texttt{\_tm\_clone} definition.  Simpler benchmarks (bank: 3 clones)
clone only the few helpers that the transaction calls directly.

\begin{table}[t]
\centering
\small
\begin{tabular}{lrrrrrrrr}
\toprule
Benchmark & \multicolumn{2}{c}{IR lines} & TX & TM & TM hooks & Clones & Redirects & Ratio \\
          & raw & instr & fns & gvs & r\slash w & defs & to clone & instr\slash raw \\
\midrule
  bank &  7371 &  7407 &   3 &   1 &   38 &    3 &      9 & 1.00 \\
  avltree &  7999 & 11031 &   5 &   1 &  213 &  178 &    410 & 1.37 \\
  avltree\_recursive &  5646 &  6369 &   5 &   1 &  110 &   14 &     64 & 1.12 \\
  stmbench7 & 20637 & 30221 &  38 &   1 &  582 &  580 &   1262 & 1.46 \\
  ycsb & 10225 & 12913 &   6 &   1 &  160 &  179 &    329 & 1.26 \\
  eigen &  7331 &  7420 &   4 &   1 &   25 &    4 &      4 & 1.01 \\
  stamp & 53241 & 75573 &  23 &   1 & 1404 & 1546 &   3072 & 1.41 \\
  bitmap &  5142 &  5293 &   3 &   1 &   30 &    5 &      9 & 1.02 \\
  hashmap &  5369 &  5804 &   5 &   1 &   64 &    9 &     11 & 1.08 \\
  list &  5269 &  5522 &   3 &   1 &   58 &    6 &      7 & 1.04 \\
  set &  5170 &  5358 &   3 &   1 &   32 &    4 &      6 & 1.03 \\
  heap &  5205 &  5426 &   3 &   1 &   42 &    9 &     10 & 1.04 \\
  rbtree &  5981 &  7034 &   5 &   1 &  210 &   15 &     44 & 1.17 \\
\bottomrule
\end{tabular}
\caption{LLVM IR instrumentation assessment (CloneOnly mode).  IR lines ---
uninstrumented vs.\ instrumented (post-plugin).  TX = transaction
functions, TM = annotated globals, TM hooks = statically inserted
\texttt{tm\_read\_*}/\texttt{tm\_write\_*} calls, Clones = cloned
helper functions, Redirects = call sites redirected to clones,
Ratio = instr / raw lines.}
\label{tab:instrumentation-assessment}
\end{table}

\section{Related Work}\label{sec:related}

\subsection{Compiler-Based TM Instrumentation}

The idea of using the compiler to automate STM instrumentation is as old as STM itself.
The 2006 HyTM proposal~\cite{HyTM} describes a ``proof-of-concept compiler'' built on the
Sun Studio back end, intercepting function calls to delimit transactions.  The Dresden TM
Compiler~\cite{DTMC} provides a full C/C++ TM compiler, but as a standalone compiler
fork rather than a plugin for an existing compiler infrastructure.  GCC's TMTS
implementation~\cite{TMTS} is the only production compiler with integrated TM support,
but its complexity (transaction-safe type system, function multiversioning, link-time
cloning) led the C++ standards committee to conclude that the TS was ``too complicated''
and needed replacement~\cite{P1875}.

More recently, the TACO 2019 paper by Zardoshti et al.~\cite{Zardoshti2019} (extended
in the C++ TM Lite proposal~\cite{P1875}) describes an LLVM plugin that intercepts
lambda expressions and RAII scope objects to delimit transactions.  The plugin is
available at \texttt{github.com/mfs409/llvm-transmem}.  This is the closest prior
work to ours, but there are several important differences:

\paragraph{API design.}  The \ZardoshtiEtAl plugin uses \texttt{TX\_BEGIN \{ ... \} TX\_END} macros
that expand to lambda expressions or RAII objects.  This requires the programmer to
restructure existing code into lambda bodies or declare local RAII variables.  Our
annotation-driven approach (\texttt{\_\_attribute\_\_((annotate(...)))}) attaches metadata
to existing function declarations and global variables, requiring zero syntactic
restructuring.  An existing class with a \texttt{transfer(from, to, amount)} method
needs only a single annotation on the function --- no lambda capture, no RAII guard,
no include of a header defining macros.

\paragraph{Call-graph cloning.}  \ZardoshtiEtAl's plugin uses conditional dispatch: every call
site checks a runtime flag to choose between the original and instrumented version
of a function.  This adds a branch per call and prevents inlining across the
instrumentation boundary.  Our plugin uses compile-time redirection: calls within
transaction context are rewritten to point directly to instrumented clones, with
no runtime dispatch overhead.  Additionally, our fixed-point propagation
algorithm (\S\ref{sec:cloning}) handles TM-traceable arguments through STL
internal functions (e.g., \texttt{\_\_tree\_left\_rotate}), which the \ZardoshtiEtAl plugin
does not address in its published description.

\paragraph{Thread lifecycle.}  \ZardoshtiEtAl's plugin does not instrument thread entry and
exit points.  Transactions running in worker threads must initialise the STM runtime
manually.  Our plugin detects \texttt{\_\_attribute\_\_((annotate("thread")))}
functions and automatically inserts \texttt{tm\_init\_thread()} and
\texttt{tm\_exit\_thread()} calls.

\textbf{Pipeline-based build.}  Figure~\ref{fig:pipeline} shows the four-stage
compilation pipeline that transforms annotated source code into a runnable binary.
In stage~1, the Clang front end compiles the annotated \texttt{.cpp} to LLVM
bitcode (\texttt{.bc}) with \texttt{-emit-llvm}.  Stage~2 loads the plugin via
\texttt{opt -load-pass-plugin} and runs the \texttt{tm-instrument} pass pipeline,
which performs both module-level initialisation and per-function load/store
rewriting; the output is instrumented bitcode (\texttt{.instr.bc}).  Stage~3
optimises the (deliberately uninlinable) instrumented IR with \texttt{opt -O3},
producing \texttt{.opt.bc}.  Finally, stage~4 links the optimised bitcode against
a single runtime \texttt{.cpp} file to produce the executable binary.  The
\texttt{tm\_pipeline.mk} include file automates all four stages; a minimal
Makefile needs only \texttt{\$(eval \$(call tm\_target, app, app.cpp, backend))}.

\begin{figure}[h]
\centering
\begin{tikzpicture}[
    node distance=1.2cm,
    box/.style={rectangle, draw, minimum width=2.8cm, minimum height=0.9cm, align=center, font=\small},
    arrow/.style={->, thick}
]
\node[box] (src) {app.cpp\\\small TM/TX annotations};
\node[box, right=of src] (bc) {\texttt{.bc}\\LLVM IR};
\node[box, right=of bc] (instr) {\texttt{.instr.bc}\\instrumented IR};
\node[box, right=of instr] (opt) {\texttt{.opt.bc}\\optimised IR};
\node[box, right=of opt] (bin) {app binary};
\draw[arrow] (src) -- node[above,font=\tiny] {\texttt{clang++ -emit-llvm}} (bc);
\draw[arrow] (bc) -- node[above,font=\tiny] {\texttt{opt -load-pass-plugin}} (instr);
\draw[arrow] (instr) -- node[above,font=\tiny] {\texttt{opt -O3}} (opt);
\draw[arrow] (opt) -- node[above,font=\tiny] {\texttt{clang++ + runtime}} (bin);
\end{tikzpicture}
\caption{Four-stage compilation pipeline.  The \texttt{TM} and \texttt{TX}
annotations on the source survive into bitcode as
\texttt{llvm.global.annotations} entries and are detected by the plugin in
stage~2.  The runtime file (e.g., \texttt{SingleGlobalLock\_runtime.cpp}) is
linked in stage~4; selecting a different backend changes only this file.}
\label{fig:pipeline}
\end{figure}

The \ZardoshtiEtAl repository includes STAMP, microbenchmarks, and memcached, but
their STAMP port covers only 6 of the 8 canonical benchmarks (missing bayes and
yada); ours covers all 8.  Their implementation is a direct port of the original
STAMP C source code with \texttt{TX\_BEGIN}/\texttt{TX\_END} macros inserted,
while ours is a from-scratch C++17 reimplementation using function-level
\texttt{TX} annotations.  Our repository
includes per-backend unit tests, a parameterised benchmark runner with fast and
standard modes, and versioned, dated result directories.  The \ZardoshtiEtAl repository
is tested only within a Docker container for LLVM 10.0 and has not been updated
since 2020; our plugin targets LLVM 16+ and builds natively on macOS and Linux.

\paragraph{STAMP benchmark coverage.}
Table~\ref{tab:stamp-comparison} compares STAMP coverage in detail.
The original specification (Cao Minh et al., IISWC 2008) defines eight
benchmarks.  Our implementation covers all eight; the \ZardoshtiEtAl repository
covers six.

\begin{table}[h]
\centering
\begin{tabular}{lcc}
\toprule
STAMP benchmark & \ZardoshtiEtAl & This work \\
\midrule
bayes (Bayesian network learning) & --- & Yes \\
genome (gene sequencing) & Yes & Yes \\
intruder (network intrusion detection) & Yes & Yes \\
kmeans (clustering) & Yes & Yes \\
labyrinth (maze routing) & Yes & Yes \\
ssca2 (graph kernels) & Yes & Yes \\
vacation (travel reservation) & Yes & Yes \\
yada (Delaunay mesh refinement) & --- & Yes \\
\bottomrule
\end{tabular}
\caption{STAMP benchmark coverage.  Our reimplementation covers all 8 benchmarks
from the original specification; the \ZardoshtiEtAl repository is missing bayes and yada.}
\label{tab:stamp-comparison}
\end{table}

\paragraph{Backend coverage.}  Both plugins support multiple STM algorithms.
\ZardoshtiEtAl's repository provides TinySTM, NOrec, and single-lock backends, with
additional PTM variants.  Our plugin provides NOrec, TL2, TinySTM (three
variants: WBCTL, WBETL, WT), SwissTM, SingleGlobalLock, and PersistentSGL,
each with a shared runtime interface and per-backend unit tests.

\paragraph{Code complexity and robustness.}
Table~\ref{tab:complexity-comparison} compares the plugin codebases.
Both plugins are comparable in size (\textasciitilde 1,800 vs.\ \textasciitilde 2,100 lines),
but ours is distributed across focused single-responsibility headers
(annotation utils, call graph, method instrumentation, local variables,
runtime hooks, thread guard) rather than a monolithic class with 15 methods.

Our plugin uses the LLVM 16+ new pass manager (\texttt{opt -load-pass-plugin
-passes="tm-instrument"}), while \ZardoshtiEtAl's uses the legacy \texttt{ModulePass}
manager (\texttt{opt -load}), deprecated since LLVM 14 and removed in LLVM 17.
\ZardoshtiEtAl's plugin explicitly documents known limitations: it ``does not currently
handle the -g flag correctly,'' assumes x86 for \texttt{long double}, has
untested code paths for indirect calls within try blocks, and uses a fragile
lambda-detection heuristic (matching the string \texttt{TM\_OPAQUE} in mangled
names).  The source code contains 7 \texttt{WARNING} comments identifying
untested or known-broken paths.  Our plugin does not rely on lambda detection,
mangled name heuristics, or x86-specific type sizes.

\begin{table}[h]
\centering
\begin{tabular}{lcc}
\toprule
Metric & \ZardoshtiEtAl~\cite{Zardoshti2019} & This work \\
\midrule
Total plugin LOC (approx.) & 2,100 & 1,812 \\
Number of source files & 12 & 9 \\
Pass manager & Legacy (deprecated) & New PM (LLVM 16+) \\
LLVM version & 5.0--10.0 & 16+ \\
Lambda detection & Mangled-name heuristic & Not required (function-level) \\
Function cloning & Conditional dispatch & Compile-time redirection \\
API coverage & Lambda + RAII + C & Function annotation \\
Thread lifecycle & Manual & Automatic \\
Known warnings in code & 7 WARNING comments & 0 \\
Debug mode (-g) & Broken & Supported \\
\bottomrule
\end{tabular}
\caption{Code complexity and robustness comparison.}
\label{tab:complexity-comparison}
\end{table}

\paragraph{Corner-case breakage risks.}
Both plugins share fundamental limitations inherent to static whole-program
analysis: inter-module calls (calls to functions defined in separately-compiled
translation units) cannot be cloned, and indirect calls through function
pointers or virtual dispatch are invisible to the static call graph.  If a
function called through an indirect site writes to TM-annotated globals, the
write bypasses the TM runtime, causing silent data corruption.

Our plugin has two additional risk categories unique to its approach.
First, the \texttt{noinline} attribute on \texttt{TX} functions is required
to preserve the annotation through LLVM's inliner before the plugin runs.
If a \texttt{TX} function is inlined into a non-TX caller before the pass
executes, it loses its transaction boundary.  Second, the
\texttt{isArgTraceable} recursion in \texttt{computeClonableFunctions} uses
a depth limit of 10; deeply nested pointer chains could exceed this limit,
causing incomplete cloning.

\ZardoshtiEtAl's plugin has several risks that ours avoids.  The lambda-detection
heuristic (\texttt{discover\_lambda\_funcs}) identifies lambdas by checking
if a function has exactly 2 arguments where one is \texttt{TM\_OPAQUE*} ---
any lambda with that signature but different semantics would be falsely
matched.  The \texttt{discover\_annotated\_funcs} method hard-codes a list
of TM runtime functions (\texttt{TM\_EXECUTE}, \texttt{TM\_EXECUTE\_C},
\texttt{setjmp}, \texttt{TM\_RAII} ctor/dtor) that must be kept in sync
with the runtime library.  The RAII API implementation
(\texttt{instrument\_raii\_regions}) transforms every instruction within
a scope into a diamond-pattern branch; this can exponentially increase
the IR size for long transactions, placing practical limits on transaction
length.  The plugin uses the deprecated \texttt{CallSite} wrapper (removed
in LLVM 14), making forward porting impossible without significant
refactoring.

\subsection{Compiler Instrumentation as an Unmet Goal}

A substantial body of TM research advocates for compiler support without
implementing it.  The STO system~\cite{STO} (EuroSys 2016) explains that a
``TM programmer (or TM compiler) labels accesses to shared memory'' but
provides only a C++ library based on operator overloading.  The ByteSTM
survey~\cite{ByteSTM} categorises ``compiler-based STM'' as one of three
implementation strategies alongside library-based and VM-based, then proceeds
with a Jikes RVM modification.  LiTM~\cite{LiTM} (PPoPP 2019) implements a
deterministic STM with no compiler component.  OneFile~\cite{OneFile} (2019)
uses C++ operator overloading to wrap types and explicitly states its approach
requires ``no compiler instrumentation.''  HaTS~\cite{HaTS} (OPODIS 2019)
collects metadata via ``developer's annotations'' at compilation time but
does not provide a compiler pass to do so.  The C++ TM Lite
proposal~\cite{P1875,P2066} explicitly states that ``compiler instrumentation
to support software and hybrid TM is more complex'' and that the proposal
``want[s] to allow more elaborate implementations based on software
transactional memory but, unlike the current TS, do not want to require such
elaborate implementations.''  Across these works, compiler-based TM
instrumentation remains a stated ideal rather than a delivered artifact.
Our plugin demonstrates that a practical, multi-backend compiler plugin for
STM is achievable with modest code size (\textasciitilde 2,500 lines of LLVM
pass code) and no changes to the Clang front end.

\subsection{TM Algorithms}

TinySTM~\cite{TinySTM} provides word-based STM with per-access logging (read-set
and write-set), which our evaluation confirms is expensive for workloads with
high read volumes (STMbench7 long traversals).  NOrec~\cite{NOrec} avoids
read-set logging and is the most practical backend for workloads dominated by
reads.  TL2~\cite{TL2} and SwissTM~\cite{SwissTM} offer intermediate trade-offs.
The TM TS~\cite{TMTS} GCC implementation bundles its own STM runtime; our plugin
separates the compiler pass from the backend, allowing any of these algorithms
to be selected at build time without modifying the instrumentation pass.
Our backend implementations are modernised ports of the original algorithms.
The original TinySTM, TL2, and SwissTM distributions used pre-C++11
synchronisation primitives: the \texttt{atomic\_ops} library, compiler builtins
(e.g., \texttt{\_\_sync\_synchronize}), and inline assembly for platform-specific
fences and CAS.  Our implementations replace these with standard
\texttt{std::atomic} (C++11), \texttt{std::memory\_order} for fine-grained
ordering, and \texttt{std::mutex}/\texttt{std::atomic\_compare\_exchange\_weak}
for lock acquisition.  The backends are header-only and require no platform-specific
code or external library dependencies beyond the C++ standard library.

\subsection{Implementation Approaches: Drawbacks of Each Solution}

We evaluate three implementation approaches that share the same STM backends:
the annotation-driven LLVM plugin, an explicit (expli) C++ template API using
\texttt{TM<T>::transaction([\&]\{\})}, and a Rust equivalent using
\texttt{TM::transaction(|tx| \{...\})} with a \texttt{TmCell<T>} wrapper.
Each approach introduces distinct drawbacks.

\paragraph{Annotation-driven LLVM plugin.}
The plugin requires no API changes to the source code beyond a \texttt{TX}
function annotation and \texttt{TM} global annotations.  This is the least
invasive approach, but it has the highest performance overhead:
\begin{itemize}
\item \textbf{Transitive call-graph cloning} duplicates every function reachable
  from a transaction --- including all STL internal helpers
  (\texttt{std::vector::push\_back}, \texttt{std::\_\_tree\_left\_rotate},
  destructors, allocators).  A simple 3-clone benchmark like bank generates 38
  TM hooks per TX; STMbench7 generates 580 clones and 1262 redirects.
\item \textbf{Function-call indirection}: every TM operation goes through a
  \texttt{\_tm\_clone} wrapper, adding a call/return cycle that the post-instrumentation
  \texttt{-O3} pass may or may not inline.  The always-inline pipeline avoids
  this indirection but instruments \emph{all} code including STL internals,
  which is slower for large transactions.
\item \textbf{LLVM pass maintenance}: the plugin targets LLVM 22 internals
  (IR builder, pass manager, analysis passes) and requires porting for each
  LLVM release.  The 2025 LLVM 22 iteration compatibility breaks from LLVM 21's
  pass registration API forced a rewrite of the module pass.
\item \textbf{Annotation fragility}: a function inline before the pass runs
  loses its TX attribute.  The plugin requires \texttt{noinline} on TX
  functions, which may conflict with LTO or profile-guided optimisation
  strategies.
\item \textbf{Opaque function detection}: overflow-buffer calls like
  \texttt{memcpy}, \texttt{memset}, \texttt{operator new}, and
  \texttt{operator delete} must be precisely intercepted.  A missed
  interception (e.g., \texttt{llvm.memset.p0.i8} with a type suffix)
  bypasses the TM runtime, causing double-free or data corruption.
\end{itemize}
Throughput on the bank benchmark at 2 threads ranges from 37K txns/s
(TinySTM/WBCTL) to 306K txns/s (TL2) under the plugin.

\paragraph{C++ expli template API.}
A separate implementation using \texttt{TM<T>::transaction([\&]\{\})} avoids
the plugin's overhead by manually wrapping transaction bodies in a lambda.
\texttt{TM<T>} is a template class that stores a typed value and provides
\texttt{tm\_read}/\texttt{tm\_write} hooks:
\begin{itemize}
\item \textbf{No compiler dependency}: the expli API is pure C++20 --- no
  LLVM pass, no cloning, no IR-level instrumentation.  Compile with any
  C++20 compiler and link any backend.
\item \textbf{No call-graph cloning}: every TM operation is a direct call
  to \texttt{tm\_read\_*}/\texttt{tm\_write\_*}, eliminating the
  \texttt{\_tm\_clone} indirection.  Bank achieves 130K txns/s (TinySTM/WBCTL),
  3.5$\times$ faster than the plugin.
\item \textbf{Lambda overhead}: the transaction closure must capture all
  shared variables, which prevents the use of \texttt{return},
  \texttt{goto}, \texttt{break}, and non-copyable types inside the
  transaction body.  The lambda itself adds a call/return cycle per
  transaction (negligible for long TXs, measurable for short ones).
\item \textbf{Template bloat}: \texttt{TM<T>} is type-aware, generating
  separate \texttt{tm\_read\_<T>}/\texttt{tm\_write\_<T>} specialisations
  for each type used in a TX.  With many distinct types (structs, enums,
  pointers), this increases compile time and binary size.
\item \textbf{Manual instrumentation scope}: the developer must identify
  every transaction boundary and wrap it in a lambda; a missed lambda risks
  unprotected concurrent access.  This is the same burden as the
  \ZardoshtiEtAl\ plugin's \texttt{TX\_BEGIN...TX\_END} macro approach.
\end{itemize}

\paragraph{Rust TM API with \texttt{WriteBack} safe commit.}
The Rust API uses \texttt{TM::transaction(|tx| \{...\})} with
\texttt{TmCell<T>} as the typed wrapper, plus a \texttt{WriteBack} enum
that encapsulates the \texttt{unsafe} in TM commit:
\begin{itemize}
\item \textbf{Unsafe confinement}: the \texttt{WriteBack} enum stores
  \texttt{(addr, value)} pairs and provides a safe \texttt{apply(self)}
  method that internally performs \texttt{unsafe \{ ptr::write \}}.
  \texttt{tm\_commit()} iterates over \texttt{write\_backs} and calls
  \texttt{wb.apply()} --- no \texttt{unsafe} blocks in commit.  This is
  the most principled encapsulation of TM's inherent memory-safety risk.
\item \textbf{Active safety guarantee}: Rust's borrow checker prevents
  concurrent access to \texttt{TmCell<T>} within a transaction, avoiding
  iterator invalidation and use-after-free at compile time --- a class of
  bugs that manifests as segfaults or double-frees in the C++ versions.
\item \textbf{\texttt{unsafe} remainder}: write-through backends (WT,
  SwissTM) still require \texttt{unsafe \{ ptr::write \}} in their
  \texttt{write\_word} functions because the immediate write cannot be
  deferred.  \texttt{TmPrimitive::tm\_read}/\texttt{tm\_write} are
  \texttt{unsafe fn} because they operate on raw \texttt{*mut T}; the
  safe wrapper is \texttt{Transaction::read}/\texttt{write} taking
  \texttt{\&TmCell<T>}.
\item \textbf{Ecosystem lock-in}: a Rust-only solution cannot instrument
  existing C++ codebases.  The Rust API is a reimplementation of the same
  backends for the Rust ecosystem, not a replacement for the LLVM plugin.
\item \textbf{Performance}: Rust's aggressive monomorphisation reduces
  type-erasure overhead compared to C++'s \texttt{any\_type\_t} union
  dispatch.  On eigenbench at 2 threads:
  Rust WBCTL = 968K txns/s, C++ expli = 525K txns/s, Rust NOrec = 1.19M
  txns/s.  The TSXSGL (single-global-lock) Rust backend reaches 5.3M
  txns/s, demonstrating minimal TM-overhead upper bound.
\end{itemize}

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrrc}
\toprule
Approach & Bank (tx/s) & EigenBench (tx/s) & LOC & Compiler dep. \\
\midrule
LLVM plugin (WBCTL) & 37K & --- & 2500 & LLVM 22 \\
LLVM plugin (TL2) & 306K & --- & 2500 & LLVM 22 \\
C++ expli (WBCTL) & 130K & 525K & 800 & None \\
Rust WBCTL & 431K & 968K & 900 & None \\
Rust NOrec & --- & 1.19M & 900 & None \\
Rust TSXSGL & --- & 5.34M & 900 & None \\
Uninstrumented (no TM) & 9.09M & --- & --- & None \\
\bottomrule
\end{tabular}
\caption{Performance comparison across implementation approaches.
Bank benchmark: 2 threads, 2000ms, 1024 accounts (uninstrumented fails
correctness).  EigenBench: 2 threads, 5000ms.  The Rust API achieves
1.5--3.3$\times$ higher throughput than C++ expli and 3.5--12$\times$
higher than the LLVM plugin.  TSXSGL represents the TM-overhead-free
upper bound (serial execution through a global spinlock).}
\label{tab:impl-comparison}
\end{table}

\paragraph{Summary of drawbacks.}
The LLVM plugin offers the lowest annotation burden but the highest
performance cost (37K--306K txns/s on bank).  The C++ expli API removes
the compiler dependency and improves throughput (130K) at the cost of
manual transaction boundary management and lambda syntactic overhead.
The Rust API provides the best throughput (431K--1.19M) and the safest
unsafe encapsulation via \texttt{WriteBack}, but cannot instrument
existing C++ code.  For new Rust projects, the \texttt{WriteBack}
approach is recommended; for existing C++ codebases, the LLVM plugin
is the only option; the C++ expli API offers a middle ground with no
compiler dependency.

\section{Conclusion}

We presented an LLVM compiler plugin for automatic STM instrumentation using
source-level annotations.  The plugin's key contributions are:

\begin{itemize}
\item Annotation-driven TX detection that requires no changes to function types
  or calling conventions.
\item A \texttt{tracesFromTMGlobal} backward tracer that correctly handles
  iterator-based element access through STL containers.
\item Fixed-point call-graph propagation that clones STL internal functions
  (e.g., \texttt{\_\_tree\_left\_rotate}) whose arguments are heap pointers
  that ultimately originate from TM globals.
\item Support for six STM backends, with evaluation showing that NOrec and
  SingleGlobalLock provide the best trade-off of correctness and performance
  for the annotation-based instrumentation approach.
\item \textbf{Modernised STM backends}: reimplementation of TinySTM, TL2, NOrec,
  and SwissTM using standard C++11 \texttt{std::atomic} and C++17,
  replacing the original pre-C++11 \texttt{atomic\_ops}, compiler builtins,
  and inline assembly.  The backends are header-only, portable across
  architectures, and require no platform-specific code or external library
  dependencies.
\item \textbf{WriteBack safe commit pattern}: a Rust API that encapsulates the
  \texttt{unsafe} inherent in TM memory writes inside a \texttt{WriteBack}
  enum, providing a safe \texttt{apply()} method and achieving 1.5--12$\times$
  higher throughput than the LLVM plugin through aggressive monomorphisation
  and elimination of call-graph cloning overhead.
\end{itemize}

\bibliographystyle{plain}
\begin{thebibliography}{99}

\bibitem{Zardoshti2019}
Pantea Zardoshti, Tingzhe Zhou, Pavithra Balaji, Michael L. Scott, and Michael Spear.
``Simplifying Transactional Memory Support in C++''.
ACM TACO 16(3), 2019.

\bibitem{NOrec}
Luke Dalessandro, Michael F. Spear, and Michael L. Scott.
``NOrec: Streamlining STM by Abolishing Ownership Records''.
PPoPP 2010.

\bibitem{TinySTM}
Pascal Felber, Christof Fetzer, and Torvald Riegel.
``Dynamic Performance Tuning of Word-Based Software Transactional Memory''.
PPoPP 2008.

\bibitem{TL2}
Dave Dice, Ori Shalev, and Nir Shavit.
``Transactional Locking II''.
DISC 2006.

\bibitem{SwissTM}
Michal Kapalka, Aleksandar Dragojevi\'c, and Maurice Herlihy.
``Stretching Transactional Memory''.
EuroSys 2009.

\bibitem{TMTS}
ISO/IEC JTC 1/SC 22/WG 21.
``Technical Specification for C++ Extensions for Transactional Memory''.
N4514, 2015.

\bibitem{HyTM}
Peter Damron, Alexandra Fedorova, Yossi Lev, Victor Luchangco, Mark Moir, and Daniel Nussbaum.
``Hybrid Transactional Memory''.
ASPLOS 2006.

\bibitem{DTMC}
Torvald Riegel.
``Software Transactional Memory for C/C++''.
PhD thesis, TU Dresden, 2019.

\bibitem{P1875}
Michael L. Scott, Michael Spear, Victor Luchangco, Pantea Zardoshti, and Tingzhe Zhou.
``Transactional Memory Lite Support in C++''.
WG21 P1875R2, 2021.

\bibitem{P2066}
Hans Boehm, Victor Luchangco, Jens Maurer, Michael L. Scott, Michael Spear, and Michael Wong.
``Suggested Draft TS for C++ Extensions for Minimal Transactional Memory''.
WG21 P2066R10, 2021.

\bibitem{STO}
Nathaniel Herman, Jeevana Priya Inala, Yihe Huang, and Eddie Kohler.
``Type-Aware Transactions for Faster Concurrent Data Structures''.
EuroSys 2016.

\bibitem{ByteSTM}
M. I. Zabi, Ali Jose Mashitzadeh, and Roberto Palmieri.
``ByteSTM: Virtual Machine-Level Java STM''.
TRANSACT 2013.

\bibitem{LiTM}
Yu Xia, Yao Xu, William S. Moses, and Srinivas Devadas.
``LiTM: A Lightweight Deterministic Software Transactional Memory System''.
PPoPP 2019.

\bibitem{OneFile}
Pedro Ramalhete and Andreia Correia.
``OneFile: A Wait-Free Universal Construction for Transactional Memory''.
2020.

\bibitem{HaTS}
Zhanhao Chen, Ahmed Hassan, Masoomeh Javidi Kishi, Jacob Nelson, and Roberto Palmieri.
``HaTS: Hardware-Assisted Transaction Scheduling''.
OPODIS 2019.

\end{thebibliography}

\end{document}
