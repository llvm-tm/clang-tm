\* GPU_SIMT — fixed protocol (Buggy = FALSE).
\* All invariants must hold. This models the CSMV GPU batch executor after the
\* 2026-07-31 fixes (lane-0-only write mutation, ballot convergence check,
\* single clock increment per commit).
SPECIFICATION Spec
INVARIANT
    ReadConsistencyOK
    VersionChainMonotonicOK
    WriteSetCountOK
    ClockMatchesCommits
    NoSyncError
    ReadSetFreshOK

CONSTRAINT TLCBound

CONSTANTS
    Warp = {1, 2}
    Lane = {0, 1}
    Addr = {0, 1}
    Value = {0, 1}
    Buggy = FALSE
