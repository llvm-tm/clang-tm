\* GPU_GUST — TLC liveness configuration
\*   java -cp /tmp/tla2tools.jar tlc2.TLC GPU_GUST.tla -config GPU_GUST-liveness.cfg
SPECIFICATION Spec_WF
INVARIANT Inv
CONSTRAINT InvCommitBudget
PROPERTY ProgressProp

CONSTANTS
    Warp = {1, 2}
    Thread = {0, 1}
    Addr = {0, 1}
    ReadsPerThread = 1
    WritesPerThread = 1
    MaxSlots = 8
    MaxCommits = 2
