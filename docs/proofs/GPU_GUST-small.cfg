\* GPU_GUST — TLC configuration (minimal, correctness smoke test)
\*   java -cp /tmp/tla2tools.jar tlc2.TLC -deadlock GPU_GUST.tla -config GPU_GUST-small.cfg
SPECIFICATION Spec
INVARIANT Inv
CONSTRAINT InvCommitBudget

CONSTANTS
    Warp = {1, 2}
    Thread = {0, 1}
    Addr = {0, 1}
    ReadsPerThread = 1
    WritesPerThread = 1
    MaxSlots = 8
    MaxCommits = 1
