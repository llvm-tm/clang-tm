\* GPU_SIMT buggy — demonstrates NoSyncError violation only.
\* (commit validates with __ballot_sync while the warp is diverged)
SPECIFICATION Spec
INVARIANT
    NoSyncError

CONSTRAINT TLCBound

CONSTANTS
    Warp = {1, 2}
    Lane = {0, 1}
    Addr = {0, 1}
    Value = {0, 1}
    Buggy = TRUE
