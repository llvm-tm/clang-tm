\* GPU_SIMT buggy — demonstrates ClockMatchesCommits violation only.
\* (clock advances by |Lane| per commit instead of 1)
SPECIFICATION Spec
INVARIANT
    ClockMatchesCommits

CONSTRAINT TLCBound

CONSTANTS
    Warp = {1, 2}
    Lane = {0, 1}
    Addr = {0, 1}
    Value = {0, 1}
    Buggy = TRUE
